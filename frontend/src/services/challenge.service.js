export const challengeService = {
  getChallenges: async () => {
    // Fetch all challenges
  },
  getChallenge: async (id) => {
    // Fetch single challenge
  },
  solveChallenge: async (id, answer) => {
    // Submit challenge answer
  }
}
export const contestService = {
  getContests: async () => {
    // Fetch all contests
  },
  registerContest: async (contestId) => {
    // Register for contest
  },
  submitContestAnswer: async (contestId, challengeId, answer) => {
    // Submit contest answer
  }
}
export const submissionService = {
  submitAnswer: async (challengeId, answer) => {
    // Submit answer logic
  },
  getSubmissionHistory: async (userId) => {
    // Get user's submission history
  }
}